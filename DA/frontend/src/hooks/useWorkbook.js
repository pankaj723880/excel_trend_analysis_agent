import { useWorkbook } from '../context/WorkbookContext'

// Re-export the context hook for convenience/backward-compat with the
// requested hooks/useWorkbook.js path.
export default useWorkbook
export { useWorkbook }
